## Step 01 — 建立作品集网页工程

时间：2026-09-22 10:03

用户要求：将现有 Rive 滚轮网页放入本地工程并发布到指定 GitHub 仓库。

修改：
- index.html
- assets/untitled.riv
- assets/rive-runtime/canvas_advanced.mjs
- assets/rive-runtime/rive.wasm

结果：已将现有网页与 Rive 资源放入工程；动画文件 SHA-256 与原文件一致。

验证：本地 HTTP 200；Rive/WASM 资源 200；桌面滚轮正向与反向操作后画面正确变化并返回起点；移动端无溢出和页面错误。PASS

遗留问题：无。

下一步：等待用户下一条指令。

## Step 02 — 发布 GitHub Pages

时间：2026-09-22 10:03

用户要求：将作品集网页发布到 Xuxudong31/Personal-Portfolio。

修改：
- 提交并推送 main 分支。
- 设置 GitHub Pages 从 main 分支根目录发布。

结果：https://xuxudong31.github.io/Personal-Portfolio/

验证：GitHub Pages 构建状态 built；线上首页、Rive、WASM 均返回 200；浏览器实测滚轮正放、倒放并返回起点，页面无错误。PASS

遗留问题：无。

下一步：等待用户下一条指令。

## Step 03 — 手机和平板触控适配（仅本地）

时间：2026-09-22 10:19

用户要求：手指滑动触发动画，并完成手机和平板适配；检查通过前不部署。

修改：
- index.html：手指上滑正放、下滑倒放；竖屏针对文字区域调整画面取景。
- index.html：使用动态视口高度并阻止触控滚动干扰动画。

结果：手机、平板竖屏和平板横屏可正常显示并触控播放；本次改动仅在本地，未推送。

验证：Chrome 模拟桌面、390×844 手机、768×1024 平板竖屏、1024×768 平板横屏；实测上滑前进、下滑返回、无溢出和页面错误；本地预览 HTTP 200。PASS

遗留问题：等待用户视觉检查。

下一步：用户确认后再提交并部署。

## Step 04 — 发布触控与响应式版本

时间：2026-09-22 10:38

用户要求：将已完成的手机和平板适配发布上线。

修改：
- 将 index.html 的触控和响应式改动推送至 main。

结果：https://xuxudong31.github.io/Personal-Portfolio/

验证：GitHub Pages 对提交 1100c94 构建成功；线上桌面滚轮、手机和平板手势均实测正放和倒放；Rive、WASM、JavaScript 资源均返回 200，无页面错误。PASS

遗留问题：无。

下一步：等待用户下一条指令。

## Step 05 — 新 Rive 文件双时间线衔接（仅本地）

时间：2026-09-22 11:46

用户要求：页面加载时自动播放新 Timeline 1，再衔接到原动画改名后的 Timeline 2。

修改：
- assets/untitled.riv：替换为用户新提供的文件。
- index.html：加载后自动播放 Timeline 1，并将滚轮交互切换到 Timeline 2。

结果：加载后自动播放 Timeline 1，结束后停在 Timeline 2 首帧；随后滚轮向下正放、向上倒放 Timeline 2。仅本地修改，尚未推送上线。

验证：新 .riv 哈希与下载源文件一致；Rive 元数据为 Timeline 1（36 帧/30 fps，1.2 秒）和 Timeline 2（30 帧/30 fps，1 秒）；浏览器截图确认开场结束画面、滚轮播放末帧；画布指纹确认开场期间持续变化、倒放后精确回到衔接帧；页面无报错。PASS

遗留问题：等待用户视觉检查。

下一步：用户确认后再部署。

## Step 06 — 调整 Timeline 1 为 0.5 倍速和指定曲线（仅本地）

时间：2026-09-22

用户要求：Timeline 1 按 30 fps 下 00:01:06 的源时长、0.5 倍速及 cubic-bezier(.87, 0, .58, 1) 播放。

修改：
- index.html：由文件元数据计算开场播放时长；按指定曲线推进 Timeline 1。

结果：Timeline 1 源动画 36 帧/30 fps，对应 1.2 秒；按 0.5 倍速播放 2.4 秒，并以 cubic-bezier(.87, 0, .58, 1) 推进。尚未部署。

验证：浏览器在开场期间多次采样画布均持续变化，2.8 秒后画面稳定；开场终帧与 Timeline 2 首帧衔接，滚轮正放后倒放回到相同画布指纹；Rive 请求 200、无页面错误；视觉截图正常。PASS

遗留问题：等待用户视觉检查。

下一步：用户确认后再部署。

## Step 07 — 更新用户修订的 Rive 动画（仅本地）

时间：2026-09-22

用户要求：检查新版 untitled (2).riv 的修改并调整网页。

修改：
- assets/untitled.riv：替换为用户新文件；旧版备份到本机临时目录。
- index.html：更新资源缓存标记；继续从文件元数据计算播放时长。

结果：新版 Timeline 1 为 32 帧/30 fps、0.5 倍速约 2.13 秒；Timeline 2 保持 30 帧/30 fps；仅本地修改。

验证：资源对比确认 Timeline 1 中间过程和 Timeline 2 帧画面有变化；新源文件与项目资源哈希一致。浏览器实际加载资源 200，开场中途与终帧画面不同、结束后稳定；滚轮正放后倒放准确回到开场终帧；无页面错误、截图显示正常。PASS

遗留问题：等待用户视觉检查。

下一步：用户确认后再部署。

## Step 08 — 校准 Timeline 1 播放速率（仅本地）

时间：2026-09-22

用户要求：以 untitled.rev 工程为准，修正网页 Timeline 1 播放速率。

修改：
- index.html：移除网页对整个开场时间轴额外添加的贝塞尔缓动；保留 0.5 倍速，由 Rive 负责文件内关键帧插值。

结果：工程文件与导出的 .riv 均确认 Timeline 1 为 32 帧/30 fps、speed=0.5，且包含多个逐关键帧 CubicEaseInterpolator；网页读取源文件 speed，以 2.133 秒匀速推进时间轴，让 Rive 自身应用逐关键帧插值，不再额外对整段施加贝塞尔缓动。尚未部署。

验证：Rive 运行时读取 speed=0.5，advance(1) 的 time=0.5；本地页面资源 HTTP 200，开场中途与终帧不同，滚轮正放与倒放后准确返回开场终帧，浏览器无页面错误；视觉截图正常。PASS

遗留问题：等待用户视觉检查。

下一步：用户确认后再部署。

## Step 09 — 发布修订后的 Rive 动画

时间：2026-09-22

用户要求：将已修正的 Timeline 1 与 Timeline 2 网页部署上线。

修改：
- 将 index.html、assets/untitled.riv 及之前的工程状态提交至 main（d441778）并推送。

结果：https://xuxudong31.github.io/Personal-Portfolio/

验证：线上 HTML、Rive、WASM、JavaScript 均为 HTTP 200；线上 Rive SHA-256 与本地一致；浏览器实测开场自动播放、滚轮正放和倒放返回起点，页面无错误。PASS

遗留问题：无。

下一步：等待用户下一条指令。

## Step 10 — 新增 Timeline 3 滚轮阶段与第 24 帧自动收尾（仅本地）

时间：2026-09-22

用户要求：使用最新 untitled.rev 和 untitled (2).riv；Timeline 2、Timeline 3 继续由滚轮控制，Timeline 3 达到第 24 帧后自动播放到末帧。

修改：
- assets/untitled.riv：替换为用户最新导出的 Rive 文件。
- index.html：按 Timeline 1 → Timeline 2 → Timeline 3 切换；Timeline 3 第 24 帧自动接续到第 30 帧；读取各时间线 speed；三段使用独立画板以避免属性残留。

结果：最新资源与三阶段交互已接入本地网页；三条时间线各用独立画板，新文件的衔接画面连续；尚未部署上线。

验证：最新资源元数据为 Timeline 1 32 帧/30 fps/0.5×、Timeline 2 30 帧/30 fps/1×、Timeline 3 30 帧/30 fps/0.3×；项目资源哈希与用户新文件一致；浏览器实测开场、两次向下滚动、第 24 帧后无需追加滚轮自动播至末帧、两次向上倒回；截图显示彩色模块正确出现，页面和资源 HTTP 200、无错误。PASS

遗留问题：等待用户视觉检查。

下一步：用户确认后再部署。

## Step 11 — Timeline 3 前 24 帧改为滚轮逐帧控制（仅本地）

时间：2026-09-22

用户要求：以最新 .rev/.riv 更新，Timeline 3 前 24 帧由滚轮正反控制，停止滚动即暂停；到第 24 帧后自动播完。

修改：
- index.html：Timeline 3 前 24 帧改为滚轮位移映射动画进度；尾段自动正放，末帧向上先回到第 24 帧。
- assets/untitled.riv：替换为用户最新导出的文件；.rev 用于核对源工程。

结果：Timeline 3 前 24 帧随滚轮正反逐步移动、停止即暂停；第 24 帧后不再需要滚轮，按文件速度自动播放到第 30 帧。未部署上线。

验证：新旧文件元数据均为 T1 32 帧/30 fps/0.5×、T2 30 帧/30 fps/1×、T3 30 帧/30 fps/0.3×；新版改动集中在 T1 开场和 T3 末帧。浏览器实测滚轮到第 4/8 帧后停住不动、向上回第 6 帧停住、达到第 24 帧后自动收尾、末帧向上回第 24 帧再逐帧倒退，最终返回 Timeline 2 起点；资源与页面 HTTP 200、无 JS 错误，末帧截图正常。PASS

遗留问题：等待用户视觉检查。

下一步：用户确认后再部署。

## Step 12 — 优化 Timeline 3 滚轮跟手感（仅本地）

时间：2026-09-22

用户要求：Timeline 3 滚轮控制不够顺畅，调整交互。

修改：
- index.html：将前 24 帧的滚轮行程从 1200px 调为 2000px，减少普通鼠标每格跳帧；滚轮事件只更新目标进度，由 requestAnimationFrame 合并绘制并短暂跟随目标；倒放尾段期间保留继续向上的滚轮位移。

结果：本地代码已更新，不改变 Rive 文件自身关键帧插值、时间线速度和第 24 帧自动收尾规则；未部署上线。

验证：浏览器实测普通滚轮输入在 45ms、125ms、405ms 有连续中间画面，随后保持静止；前段正反滚轮及停滚、到 24 帧自动播到末帧、末帧向上倒回 24 帧后保持、再向下重播、倒放期间连续滚轮输入保留、倒回 Timeline 2 均通过；无页面异常，终帧截图正常。PASS

遗留问题：等待用户主观检查滚轮手感。

下一步：用户确认后再部署。

## Step 13 — 发布 Timeline 3 滚轮优化版

时间：2026-09-22

用户要求：把当前版本发布到原有网址，供无需 VPN 的网络环境尝试访问。

修改：将本地已验证的 index.html、assets/untitled.riv 提交并推送至 main（a78786c）；不修改动画交互。

结果：https://xuxudong31.github.io/Personal-Portfolio/ 已发布最新 Timeline 3 滚轮优化版。

验证：线上 HTML 200 且包含新版滚轮逻辑；线上 Rive 文件 SHA-256 与本地一致，JS/WASM 均为 200。浏览器直接访问公开网址，实测开场、Timeline 2、Timeline 3 滚轮进退和停滚、第 24 帧自动收尾及倒回均通过，无请求失败和页面异常。PASS

遗留问题：GitHub Pages 是否免 VPN 可访问取决于访问者所在网络，本机当前网络可直接打开，但无法代替用户验证其朋友所在网络。

下一步：等待用户反馈。

## Step 14 — 准备国内可直连域名与托管

时间：2026-09-22

用户要求：不要依赖 GitHub Pages，改为朋友在国内无需 VPN 可访问的域名。

修改：无代码变更。核查项目无国内域名配置，当前终端无腾讯云、阿里云或 Gitee 的登录工具与环境凭证；筛选腾讯云 CloudBase 静态托管作为适配现有纯静态项目的方案。

结果：尚未开通国内托管或注册域名。CloudBase 默认域名仅适用于开发测试；正式公开使用自定义域名需要用户云账号及域名/备案信息。

验证：GitHub Pages 现有版本仍可用；国内托管未部署，无法验证。BLOCKED

遗留问题：需用户提供可登录的国内云平台账号，并说明是否已有可用域名；不得索取密码或密钥文本。

下一步：获得账号授权后部署静态文件并验证国内新网址。

## Step 15 — 接入“信息框架”鼠标悬停时间轴（仅本地）

时间：2026-09-22

用户要求：用新 .rev/.riv 内的信息框架悬停动画替换左上深蓝色块，1:1 覆盖且文字位置不动。

修改：
- assets/untitled.riv：替换为用户新导出的 `untitled (2).riv`；原版本仍保留在 Git 历史中。
- index.html：按原画板坐标检测信息框架区域，鼠标悬停时按新时间轴速度正放、离开时倒放；新动画与 Timeline 3 共用画板和缩放，未另建浮层。

结果：触发逻辑已接入本地；暂未部署。当前用户导出的“信息框架”动画在 0–30 帧的逐帧画面完全一致，仍无法得到要求的可见悬停替换效果。

验证：新 .riv 与源文件 SHA-256 一致；本地浏览器实测鼠标进入色块后悬停时间轴 0→1、离开后 1→0，悬停检测和 2 秒速度正确；原 Timeline 2/3 滚轮与第 24 帧倒回正常，页面无错误，终帧截图版式和文字未动。但悬停前、中、后画布指纹完全一致，视觉替换未实现。PARTIAL

遗留问题：需要在 Rive 工程中确认新增动画的关键帧作用于可见图层、并重新导出有可见帧变化的 .riv。

下一步：复核本地触发和原有滚轮动画；收到修正导出文件后再验收视觉覆盖。

## Step 16 — 接入信息框架状态机（仅本地）

时间：2026-09-22

用户要求：确认新加的状态机是否能实现信息框架鼠标悬停动画。

修改：
- assets/untitled.riv：更新为最新 `untitled (1).riv`。
- index.html：将手动驱动旧“信息框架”时间轴改为驱动 `State Machine 1` 的 `xuanting ` 布尔输入，保留原有滚轮与画板布局。

结果：状态机已接入本地页面，左上信息框架区域悬停播放进入动画、移出播放退出动画；尚未部署。

验证：浏览器实测滚轮进入最终页面、悬停/移出色块，画面正常切换、无加载或 JS 错误；悬停变化的 180,830 个像素均在画板坐标 x=52–389、y=63–597 内，文字位置和其它版块不变。向上滚轮可倒回 Timeline 3 并在停滚后停帧。移出后视觉恢复，导出动画终帧与初始帧仍有 414 个线条像素差异，均在信息框架内部。PASS

遗留问题：如需终帧与初始帧逐像素一致，应在 Rive 中微调“信息框架（鼠标退出）”最后一帧；目前肉眼无明显差异。在线版尚未更新。

下一步：用户确认本地效果后再部署。

## Step 17 — 恢复本地预览服务

时间：2026-09-22

用户要求：排查本地预览链接打不开。

修改：未改页面代码；重启失去响应的 4190 本地 HTTP 服务，改用独立隐藏进程并将输出写入临时日志。

结果：http://127.0.0.1:4190/ 已恢复；仅本机可访问，线上版未更新。

验证：首页 HTTP 200、Rive 文件 HTTP 200；浏览器加载成功，未出现兜底错误界面或脚本错误。PASS

遗留问题：本地服务随电脑重启可能停止。

下一步：用户查看本地效果，确认后再部署。

## Step 18 — 调研策划悬停动画连续反转（仅本地）

时间：2026-09-23

用户要求：鼠标快速进入/退出“调研策划”时，不从头重播；应从当前百分比直接反向。

修改：
- assets/untitled.riv：更新为 Downloads 中与新源工程对应的 `untitled (2).riv`。
- index.html：读取“调研策划（鼠标进去）”的真实 0–5 帧工作区并保存连续进度；鼠标反向时仅改变进度方向，不重新创建或切换动画实例。
- index.html：兼容新版“信息框架”状态机名称，并在状态切换时重置帧计时，避免静置后首帧跨步。
- `untitled (1).rev` 仅用于核对源动画和状态机；未对 Rive 原生二进制工程做不安全的直接改写。

结果：调研策划进入约 50% 时移出可从当前帧连续倒放；退出约 20% 时再次移入可从当前帧连续正放，关键帧插值沿用 Rive 文件。尚未部署。

验证：浏览器实测快速往返、完整进入和完整退出，无加载或脚本错误；中途反转采样颜色保持在中间态，没有跳回黄色或黑色端点。完整悬停变化严格位于画板 x=409–1006、y=63–460，区外变化像素为 0；退出后与初始截图逐像素一致。原信息框架悬停仍能触发。PASS

遗留问题：网页已解决；若还要让 Rive 编辑器预览自身也具备同样反转，需要在源工程中改成单一动画状态，并把播放 Speed 数据绑定为 +1/-1。

下一步：用户在本地预览确认效果；确认后再部署。

## Step 19 — 八个悬停模块连续反转（仅本地）

时间：2026-09-23

用户要求：按最新 `.rev/.riv` 中八个状态机接入全部悬停交互；快速进入/退出时必须从当前百分比反向，不能重播或跳帧。

修改：
- assets/untitled.riv：更新为 15:07 的最新 `Downloads/untitled.riv`（SHA-256 `AD7682...3917E`）。
- index.html：接入信息框架、调研、logo、字体、动效、图标、颜色、影像共八个 Rive 状态机；状态机只提供作者设置的鼠标命中区域。
- index.html：每个模块独立保存当前进度，并用对应“鼠标进入”时间轴连续正放/倒放；反向时不切状态、不重建动画实例。
- index.html：按各动画 speed、fps、work area 自动计算真实播放时间；调研继续使用源文件 0–5 帧工作区。

结果：八个模块全部支持从任意当前百分比立即反向；尚未部署。源 `.rev` 未做不安全的二进制改写。

验证：
- 八个状态机的实际命中区域均能触发，移出后鼠标样式恢复。
- 八个模块完整进入均有可见变化；完整退出后的截图均与初始截图逐像素一致。
- 进入约 50% → 退出 20% → 再进入 10% 的实测进度约为 `0.50 → 0.30 → 0.40`，八组全部通过，无进度重置。
- 32 次跨模块快速移动后全部恢复初始画面；无 JS/WASM 错误。
- Timeline 3 倒回可停帧，再向下滚动可回到最终初始画面。PASS

遗留问题：直接在 Rive 编辑器中播放原状态机仍会采用源工程自身的状态切换逻辑；本次修复作用于网页运行时。在线版尚未更新。

下一步：用户本地检查，确认后再部署。

## Step 20 — 更新 Timeline 1/2 与加快 LOGO 复原（仅本地）

时间：2026-09-23

用户要求：采用最新 `.riv` 中 Timeline 1/2 隐藏多余两点及修改后的 LOGO 进入动画；只加快 LOGO 移出后的复原速度，其它区域不改。

修改：
- assets/untitled.riv：更新为 `Downloads/untitled (1).riv`（SHA-256 `031F12...FDA45`）。
- index.html：读取新版 LOGO 进入动画 0–21 帧工作区；仅为 LOGO 设置 2 倍反向复原速度。
- index.html：更新 Rive 资源缓存版本；其它七个悬停模块及滚轮逻辑未改。

结果：新版 Timeline 1/2 和 LOGO 关键帧已进入网页；LOGO 完整进入约 1.4 秒，移出后约 0.7 秒恢复。尚未部署。

验证：LOGO 完整进入可见并变黑，0.7 秒退出后截图与初始画面逐像素一致；快速反向实测进度约 `0.51 → 0.30 → 0.39`，无重置；页面无加载或脚本错误。PASS

遗留问题：无。在线版尚未更新。

下一步：用户本地检查。

## Step 21 — 更新 LOGO 动画内容（仅本地）

时间：2026-09-23

用户要求：采用最新 `Downloads/untitled.riv` 中主要针对 LOGO 的修改，其它交互保持不变。

修改：
- assets/untitled.riv：更新为最新文件（SHA-256 `D4CCFBA4...FE86747`）。
- index.html：仅更新 Rive 资源缓存版本；LOGO 动画名称、状态机映射、连续反向及 2 倍复原速度均保持不变。

结果：新版 LOGO 内部关键帧已接入网页；其它模块和滚轮逻辑未改，尚未部署。

验证：本地页面和新版资源均 HTTP 200；LOGO 完整进入进度到 1，移出 350ms 后约 0.50、约 0.7 秒后回到 0；快速折返实测 `0.51 → 0.11 → 0.20`，没有重置；复原截图与初始截图逐像素一致；无 JS/WASM 错误。PASS

遗留问题：无。

下一步：完成本地实测后更新为 PASS。

## Step 22 — 发布新版作品集

时间：2026-09-23

用户要求：将当前最新版本部署上线，并将站点名称改为“Ethen个人作品集未完成版”。

修改：
- index.html：浏览器标签标题更新为“Ethen个人作品集未完成版”。
- 准备将当前最新版网页与 Rive 资源提交并推送到 GitHub Pages 的 main 分支。

结果：最新版已发布至 `https://xuxudong31.github.io/Personal-Portfolio/`，线上标题为“Ethen个人作品集未完成版”。

验证：提交 `144dfa0` 的 GitHub Pages 构建成功；线上首页、Rive 资源均 HTTP 200，线上 Rive 文件大小与 SHA-256 和本地完全一致；公网浏览器实测滚轮进入最终页面、LOGO 悬停和移出恢复正常，复原截图与初始截图逐像素一致，无 JS/WASM 错误。PASS

遗留问题：无。

下一步：等待用户检查线上效果。

## Step 24 — 接入作品集访问统计与数据后台

时间：2026-09-23

用户要求：参照“脉冲音格”的方式，为作品集增加访问统计后台；同一访客重复访问只计算一次独立访客。

修改：
- 新增 `analytics.mjs`、`server.mjs`：沿用 Render + PostgreSQL 架构，提供匿名访问记录、心跳、趋势统计和受密码保护的后台 API；数据表使用 `portfolio_analytics_*` 独立前缀，避免与“脉冲音格”数据冲突。
- 新增 `admin.html`、`admin.css`、`admin.js`：提供今日/累计独立访客、浏览量、在线人数、回访、趋势、来源、设备、浏览器和最近访问界面。
- 新增 `assets/analytics-client.js`：浏览器保存匿名 ID；同一浏览器重复访问累计浏览量，但数据库唯一访客只保留一条。统计失败不会影响 Rive。
- 新增 `render.yaml`、Node 配置与自动化测试；服务端密钥仅通过 Render 环境变量提供，不进入仓库。

结果：后台与统计功能已完成并以提交 `376f62f` 推送；GitHub Pages 前台已加载统计脚本。Render PostgreSQL 和后台服务尚未在用户账号内确认创建。状态：PARTIAL

验证：
- `npm test`：5 项全部通过，包括访客去重、请求重试去重、数据库故障返回可重试状态、同站来源、后台 Cookie、CORS、未授权拦截和源码文件保护。
- 真实浏览器连续打开/刷新两次：累计独立访客 `1`、浏览量 `2`、回访访客 `1`；后台登录、趋势与列表正常。
- Rive 回归：页面/Rive/统计接口分别返回 200/200/202，滚轮动画与新英文正常，无 JS/WASM 错误。PASS
- GitHub Pages 对 `376f62f` 构建成功；线上首页、统计脚本、Rive、WASM 和后台跳转页均返回 200。

遗留问题：需要在用户的 Render 账号中创建 Blueprint、设置 `ADMIN_PASSWORD`，并确认线上后台显示 `PostgreSQL 持久存储`。同一 Render workspace 只能有一个免费 PostgreSQL；若“脉冲音格”已占用，需要改用另一 workspace、付费库，或手动连接现有库。Render 免费 PostgreSQL 有保存期限，不适合作为长期档案；长期保留数据需后续升级数据库或接入其它持久 PostgreSQL。

下一步：用户在 Render Blueprint 页面设置 `ADMIN_PASSWORD` 并确认部署；完成公网 PostgreSQL 去重与后台实测后更新为 PASS。

## Step 23 — 替换蓝色页面英文并发布

时间：2026-09-23

用户要求：将蓝色区域原英文替换为“Turning complex ideas into clear digital experiences.”，完成后直接部署上线。

修改：
- index.html：缓存三个 Rive 画板的文本 Run，在每次时间轴应用后精确匹配目标文本并替换为用户提供的新文案；字体、排版、位置和动画参数保持不变。
- index.html：文本 Run 句柄仅在加载时创建、画板释放前集中清理，避免逐帧创建句柄。

结果：新文案已发布至 `https://xuxudong31.github.io/Personal-Portfolio/`，其它动画和页面内容保持不变。

验证：本地 1920×1080 真实浏览器实测 Timeline 1 原文案未受影响；滚轮进入 Timeline 2 后蓝色区域准确显示两行新文案，无溢出、无旧文案、无脚本错误；Timeline 3 同样保持新文案。提交 `3793eb1` 的 GitHub Pages 构建成功；公网页面 HTTP 200，真实浏览器再次滚动到蓝色区域后显示正确，fallback 隐藏且无 JS/WASM 错误。PASS

遗留问题：无。

下一步：等待用户检查线上效果。
