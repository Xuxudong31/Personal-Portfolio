# AGENTS.md — 项目执行与自验证规则

> 目的：让编码代理以**小步、可验证、可回退、低消耗**的方式完成任务。  
> 本文件不是视觉设计规范，而是项目开发过程中的工作规则。

---

## 1. 总原则

每次收到用户指令后，必须遵循以下顺序：

```text
理解需求
→ 检查当前状态
→ 选择最简单可行方案
→ 小步修改
→ 保存当前进度
→ 自己运行 / 验证
→ 修复发现的问题
→ 再次验证
→ 通过后再交付
```

禁止出现：

```text
修改完代码
→ 未运行
→ 未验证
→ 直接告诉用户“已经完成”
```

**没有验证过，就不能声称完成。**

---

# 2. 简单方案优先

任何任务都遵循：

```text
Simple First
```

优先级：

```text
现有功能
> 小修改
> 复用已有组件
> 原生能力
> 已安装依赖
> 新写少量代码
> 新增依赖
> 重构
> 复杂架构
```

如果 20 行代码可以解决：

不要写 200 行。

如果普通 CSS 可以解决：

不要为了炫技引入复杂动画库。

如果现有组件可以改：

不要重新造一个新组件。

如果当前需求只需要一个页面：

不要提前设计完整系统。

---

# 3. 禁止过度设计

除非用户明确要求，否则不要主动：

- 重构整个项目
- 改项目架构
- 换框架
- 增加大型依赖
- 创建大量抽象层
- 创建复杂状态管理
- 创建后端服务
- 创建数据库
- 增加用户没有要求的功能
- 为“未来可能用到”提前开发

只解决：

```text
当前用户明确提出的问题
```

---

# 4. 每一步必须保存状态

任务执行过程中，不要连续修改很久才保存。

每完成一个可以独立成立的步骤，都要留下一个 checkpoint。

默认使用：

```text
AGENT_STATE.md
```

记录格式：

```md
## Step 01

目标：
完成首页 Hero 布局。

已修改：
- src/pages/index.tsx
- src/styles/home.css

当前结果：
Hero 基础布局已完成。

验证：
npm run build
PASS

下一步：
加入滚动动画。
```

下一步完成后继续追加：

```md
## Step 02
...
```

---

# 5. AGENT_STATE.md 只记录必要信息

不要把大量思考过程写进去。

只保存：

```text
目标
修改了什么
当前状态
验证方式
验证结果
下一步
```

不要记录：

- 长篇推理
- 无用日志
- 重复代码
- 大段终端输出
- 与任务无关的信息

目标是：

**下次即使上下文丢失，也可以快速知道项目做到哪里。**

---

# 6. 每次修改前先读取状态

每次开始新的用户指令：

先查看：

```text
AGENT_STATE.md
```

然后只读取与当前任务有关的文件。

不要每次重新扫描整个项目。

---

# 7. 小步修改

复杂任务必须拆成小步骤。

例如用户要求：

```text
做一个带 Rive 滚轮交互的作品集首页
```

不要一次改完所有内容。

应该：

```text
Step 1
搭页面结构
→ 验证

Step 2
加入 Rive
→ 验证

Step 3
绑定滚轮
→ 验证

Step 4
加入响应式
→ 验证

Step 5
最终整体检查
```

每一步完成后更新：

```text
AGENT_STATE.md
```

---

# 8. 修改前先确认现状

开始改代码前，先确认：

- 当前项目是否能运行
- 使用什么框架
- 已经有哪些依赖
- 当前页面结构
- 是否已经存在类似功能

禁止在不知道当前状态的情况下直接重写。

---

# 9. 最小读取原则

为了减少 Token 和时间消耗：

只读取当前任务需要的内容。

例如修改：

```text
Header
```

优先只看：

```text
Header.tsx
header.css
相关 layout
```

不要直接读取：

```text
整个 src
整个 node_modules
所有页面
```

搜索优先于全文读取。

---

# 10. 最小修改原则

如果用户说：

```text
把按钮改成蓝色
```

只改按钮相关代码。

不要顺便：

- 修改页面布局
- 调整字体
- 重构 CSS
- 修改其他按钮
- 统一整个项目颜色

除非这是完成任务的必要条件。

---

# 11. 保留已有正确内容

默认原则：

```text
没有要求修改的内容，不动。
```

尤其是：

- 已确认的 UI
- 已完成的交互
- 用户提供的图片
- 用户已有文案
- 已工作的逻辑
- 已存在的数据

避免修一个地方破坏另一个地方。

---

# 12. 不随意删除

删除以下内容前必须确认它确实无用：

- 文件
- 依赖
- 组件
- 数据
- 用户内容
- 配置

如果只是暂时不用：

优先保留。

---

# 13. 每次修改后必须自验证

完成当前指令后，必须自己验证。

验证等级：

## Level 1 — 基础检查

至少检查：

```text
语法
类型
明显报错
文件引用
路径
```

---

## Level 2 — 运行检查

如果项目可以运行：

必须运行项目或相关命令。

例如：

```bash
npm run dev
npm run build
npm test
npm run lint
```

根据项目实际情况选择。

不要无脑全部运行。

---

## Level 3 — 功能检查

验证用户要求本身。

例如用户说：

```text
鼠标滚轮控制 Rive 动画
```

不能只验证：

```text
页面能打开
```

还必须验证：

```text
滚轮向下 → 动画前进
滚轮向上 → 动画后退
动画不会自动播放
不会超出 0–100%
页面无明显报错
```

---

# 14. UI 任务必须做视觉检查

如果修改的是 UI：

除了编译成功，还要检查：

- 页面是否正常显示
- 是否溢出
- 是否错位
- 是否遮挡
- 图片比例是否正常
- Desktop 是否正常
- Mobile 是否明显崩坏

如果具备浏览器 / 截图能力：

必须实际打开页面检查。

---

# 15. 交互任务必须真实操作

如果是：

- 点击
- Hover
- Scroll
- Drag
- Menu
- Modal
- Rive
- 动画
- 表单

必须真实操作一次。

不能只通过阅读代码判断“应该能用”。

---

# 16. 验证失败必须自己修

如果测试失败：

```text
不要立即交付用户。
```

执行：

```text
定位问题
→ 最小修复
→ 再次运行
→ 再次验证
```

直到：

```text
PASS
```

或者确认当前环境无法验证。

---

# 17. 无法验证时必须说明

如果因为环境原因无法验证：

不要写：

```text
已经完全正常
```

应该明确写：

```text
代码已完成。
当前环境无法验证 X，因此 X 尚未经过实际运行确认。
```

禁止假装测试通过。

---

# 18. 只运行最相关的测试

为了节省时间和 Token：

优先：

```text
最小相关测试
```

例如只改一个组件：

优先测试这个组件。

确定没有问题后，再根据风险决定是否：

```text
npm run build
```

不要每改一行代码都完整跑所有测试。

---

# 19. 最终交付前检查

每次准备回复用户前，检查：

```text
[ ] 用户要求是否全部完成
[ ] 是否只修改了必要内容
[ ] 是否保存了 checkpoint
[ ] 程序是否成功运行
[ ] 核心功能是否实际验证
[ ] 是否有报错
[ ] 是否破坏已有功能
[ ] 是否有多余依赖
[ ] 是否存在明显更简单的方法
```

全部通过后再交付。

---

# 20. 交付回复保持简洁

完成后回复：

```text
已完成。

这次修改：
- xxx
- xxx

验证：
- xxx：通过
- xxx：通过

文件：
xxx
```

不要输出：

- 大量思考过程
- 长篇技术解释
- 全部终端日志
- 用户没有问的细节

如果用户需要，再展开。

---

# 21. Token 使用规则

默认目标：

```text
少读
少写
少解释
多验证
```

具体要求：

- 不重复读取刚看过的文件
- 不重复解释已经确认的结论
- 搜索定位后只打开相关代码
- 不输出完整大型文件，除非确实需要
- 修改局部时使用局部编辑
- 不为了展示能力写复杂实现
- 不生成用户没有要求的文档
- 不做无意义的多方案比较

---

# 22. 遇到多个方案时

默认选择：

```text
实现成本最低
风险最低
最容易验证
最容易维护
```

的方案。

只有当简单方案无法满足需求时，才升级复杂度。

---

# 23. 新增依赖规则

新增 npm / pip / 其他依赖前先问：

```text
现有依赖能不能做？
原生能力能不能做？
几十行代码能不能做？
```

如果能：

不要新增依赖。

只有明显更合理时才增加。

---

# 24. Bug 修复规则

遇到 Bug：

```text
1. 先复现
2. 找最小原因
3. 做最小修复
4. 再复现原场景
5. 确认 Bug 消失
6. 检查有没有引入新问题
```

不要：

```text
看到 Bug
→ 猜原因
→ 大规模重写
```

---

# 25. 用户反馈优先

如果用户说：

```text
不是这个
```

优先理解：

```text
具体哪里不符合
```

然后修改差异。

不要：

```text
重新推翻整个项目
```

除非用户要求重新做。

---

# 26. 已确认方案不可自行改变

一旦用户确认：

- 布局
- 风格
- 颜色
- 交互
- 技术方案
- 页面结构

后续默认保持。

除非新要求与原方案冲突。

---

# 27. 数据与文件安全

任何操作都遵循：

```text
不覆盖重要数据
不破坏用户源文件
```

修改重要文件前：

优先保留原文件或保证可回退。

---

# 28. Git 项目规则

如果项目已经使用 Git：

稳定的小阶段完成后可以创建 checkpoint commit。

示例：

```bash
git add .
git commit -m "checkpoint: complete hero layout"
```

但不要为了每一行代码都 commit。

如果项目没有 Git：

不要因为这个规则强行初始化 Git。

继续使用：

```text
AGENT_STATE.md
```

记录状态即可。

---

# 29. 不把 checkpoint 文件做复杂

默认只需要一个：

```text
AGENT_STATE.md
```

不要自己创建：

```text
logs/
history/
agent-memory/
checkpoints/
snapshots/
temp-state/
```

除非项目真的需要。

保持简单。

---

# 30. 每次新指令的标准流程

收到用户新指令后执行：

```text
01 读取 AGENT_STATE.md
02 查看与任务相关文件
03 确认当前程序状态
04 选择最简单实现
05 修改
06 保存 checkpoint
07 运行相关验证
08 功能实际检查
09 有问题则修复
10 再验证
11 更新 checkpoint 为 PASS
12 回复用户
```

---

# 31. Checkpoint 模板

每完成一个阶段追加：

```md
## Step XX — 标题

时间：
YYYY-MM-DD HH:mm

用户要求：
一句话概括。

修改：
- file
- file

结果：
一句话概括当前结果。

验证：
- command / operation
- PASS / FAIL

遗留问题：
无

下一步：
等待用户下一条指令。
```

---

# 32. 任务完成状态

只能使用三种状态：

```text
PASS
PARTIAL
BLOCKED
```

定义：

### PASS

```text
已实现
已运行
已验证
符合用户描述
```

### PARTIAL

```text
部分完成
仍有未验证或未实现内容
```

### BLOCKED

```text
由于文件、权限、环境或缺失信息无法继续
```

禁止模糊描述。

---

# 33. 最重要的规则

永远记住：

```text
不要先追求复杂。
先追求正确。
```

然后：

```text
不要先告诉用户完成。
先自己证明它完成。
```

最后：

```text
每次只做当前需要做的事。
```
